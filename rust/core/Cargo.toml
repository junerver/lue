[package]
name = "lue_core"
version = "0.1.0"
edition = "2021"
description = "Pure-Rust document core for the lue reader: text pipeline, TXT indexing and layout engine"

[dependencies]
fancy-regex = "0.14"
regex = "1.13.1"
encoding_rs = "0.8.35"
thiserror = "2"
